apiVersion: v1
kind: Namespace
metadata:
  name: __PREVIEW_NS__
  labels:
    app.kubernetes.io/name: mock-me
    app.kubernetes.io/component: preview
    mock-me.dasmlab.org/preview-owner: "__PREVIEW_OWNER__"
    pod-security.kubernetes.io/enforce: baseline
    pod-security.kubernetes.io/audit: baseline
    pod-security.kubernetes.io/warn: baseline

---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: mock-me-sa
  namespace: __PREVIEW_NS__
imagePullSecrets:
  - name: dasmlab-ghcr-pull

---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: openshift-gitops-argocd-application-controller
  namespace: __PREVIEW_NS__
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: admin
subjects:
  - kind: ServiceAccount
    name: openshift-gitops-argocd-application-controller
    namespace: openshift-gitops

---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: system:openshift:scc:anyuid
  namespace: __PREVIEW_NS__
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: system:openshift:scc:anyuid
subjects:
  - kind: ServiceAccount
    name: mock-me-sa
    namespace: __PREVIEW_NS__

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mock-me
  namespace: __PREVIEW_NS__
  labels:
    app: mock-me
    mock-me.dasmlab.org/preview-owner: "__PREVIEW_OWNER__"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mock-me
  template:
    metadata:
      labels:
        app: mock-me
    spec:
      serviceAccountName: mock-me-sa
      securityContext:
        fsGroup: 65532
        fsGroupChangePolicy: OnRootMismatch
      imagePullSecrets:
        - name: dasmlab-ghcr-pull
      containers:
        - name: mock-me
          image: ghcr.io/dasmlab/mock-me:__VERSION__
          imagePullPolicy: Always
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            runAsUser: 65532
            runAsGroup: 65532
          args:
            - serve
            - --listen
            - ":8080"
            - --data-dir
            - /data
          env:
            - name: DATA_DIR
              value: /data
            - name: LISTEN_ADDR
              value: ":8080"
            - name: BUILD_VERSION
              value: "__VERSION__"
            - name: APP_PUBLIC_URL
              value: "https://__PREVIEW_HOST__"
            - name: KEYCLOAK_URL
              value: "https://keycloak.apps.2026-prod-1.ocp.dasmlab.org"
            - name: KEYCLOAK_REALM
              value: dasmlab
            - name: OIDC_CLIENT_ID
              value: mock-me
            - name: OIDC_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: mock-me-oidc
                  key: client-secret
                  optional: false
            - name: OIDC_REDIRECT_URI
              value: "https://__PREVIEW_HOST__/api/v1/auth/callback"
            - name: OIDC_CA_FILE
              value: /etc/oidc/ca.crt
            - name: INVENTORY_SSH_KEY
              value: /var/run/inventory-ssh/id_ecdsa
            - name: PULL_SECRET_FILE
              value: /var/run/secrets/openshift/pull-secret
            - name: CHEAPCLOUD_URL
              value: "https://cheapcloud-dasmlab.apps.2026-prod-1.ocp.dasmlab.org"
            # Activity log viewers (dasm by default; same as prod).
            - name: ACTIVITY_VIEWERS
              value: "dasm"
          volumeMounts:
            - name: mock-me-data
              mountPath: /data
            - name: oidc-ca
              mountPath: /etc/oidc
              readOnly: true
            - name: inventory-ssh
              mountPath: /var/run/inventory-ssh
              readOnly: true
            - name: ocp-pull-secret
              mountPath: /var/run/secrets/openshift
              readOnly: true
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              memory: "128Mi"
              cpu: "50m"
            limits:
              memory: "512Mi"
              cpu: "500m"
      volumes:
        - name: mock-me-data
          persistentVolumeClaim:
            claimName: mock-me-data
        - name: oidc-ca
          configMap:
            name: mock-me-oidc-ca
            items:
              - key: ca.crt
                path: ca.crt
            optional: true
        - name: inventory-ssh
          secret:
            secretName: mock-me-ssh
            defaultMode: 0440
            optional: true
        - name: ocp-pull-secret
          secret:
            secretName: mock-me-ocp-pull
            items:
              - key: pull-secret.json
                path: pull-secret
            optional: true

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mock-me-data
  namespace: __PREVIEW_NS__
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: __STORAGE_CLASS__
  resources:
    requests:
      storage: 2Gi

---
apiVersion: v1
kind: Service
metadata:
  name: mock-me
  namespace: __PREVIEW_NS__
spec:
  selector:
    app: mock-me
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP

---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: mock-me
  namespace: __PREVIEW_NS__
  labels:
    app: mock-me
spec:
  host: __PREVIEW_HOST__
  to:
    kind: Service
    name: mock-me
    weight: 100
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None
