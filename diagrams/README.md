# Diagrams

Sources are `*.d2`. CI renders sibling `*.svg` on push (see `.github/workflows/d2-diagrams.yml`).

| File | Purpose |
|------|---------|
| `workflow.d2` | Hub → ACM → managed compact cluster |
| `architecture.d2` | CLI / provider interface / ACM split |
